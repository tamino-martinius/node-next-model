{
  "name": "@next-model-demos/mysql-node",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --experimental-strip-types demo.ts",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down -v"
  },
  "dependencies": {
    "@next-model/core": "workspace:*",
    "@next-model/mysql-connector": "workspace:*",
    "mysql2": "^3.11.5"
  },
  "engines": {
    "node": ">=22.6"
  }
}
