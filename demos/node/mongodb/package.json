{
  "name": "@next-model-demos/mongodb-node",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --experimental-strip-types demo.ts",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down -v"
  },
  "dependencies": {
    "@next-model/core": "workspace:*",
    "@next-model/mongodb-connector": "workspace:*",
    "mongodb": "^6.10.0"
  },
  "engines": {
    "node": ">=22.6"
  }
}
